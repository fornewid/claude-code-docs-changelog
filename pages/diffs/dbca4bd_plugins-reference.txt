diff --git a/docs/plugins-reference.md b/docs/plugins-reference.md
index 48911f2..8264854 100644
--- a/docs/plugins-reference.md
+++ b/docs/plugins-reference.md
@@ -26,7 +26,7 @@ Plugins add skills to Claude Code, creating `/name` shortcuts that you or Claude
 
 **Skill structure**:
 
-```text  theme={null}
+```text theme={null}
 skills/
 ├── pdf-processor/
 │   ├── SKILL.md
@@ -54,7 +54,7 @@ Plugins can provide specialized subagents for specific tasks that Claude can inv
 
 **Agent structure**:
 
-```markdown  theme={null}
+```markdown theme={null}
 ---
 name: agent-name
 description: What this agent specializes in and when Claude should invoke it
@@ -88,7 +88,7 @@ Plugins can provide event handlers that respond to Claude Code events automatica
 
 **Hook configuration**:
 
-```json  theme={null}
+```json theme={null}
 {
   "hooks": {
     "PostToolUse": [
@@ -154,7 +154,7 @@ Plugins can bundle Model Context Protocol (MCP) servers to connect Claude Code w
 
 **MCP server configuration**:
 
-```json  theme={null}
+```json theme={null}
 {
   "mcpServers": {
     "plugin-database": {
@@ -200,7 +200,7 @@ LSP integration provides:
 
 **`.lsp.json` file format**:
 
-```json  theme={null}
+```json theme={null}
 {
   "go": {
     "command": "gopls",
@@ -214,7 +214,7 @@ LSP integration provides:
 
 **Inline in `plugin.json`**:
 
-```json  theme={null}
+```json theme={null}
 {
   "name": "my-plugin",
   "lspServers": {
@@ -290,7 +290,7 @@ The manifest is optional. If omitted, Claude Code auto-discovers components in [
 
 ### Complete schema
 
-```json  theme={null}
+```json theme={null}
 {
   "name": "plugin-name",
   "version": "1.2.0",
@@ -358,7 +358,7 @@ agent `agent-creator` for the plugin with name `plugin-dev` will appear as
 
 The `userConfig` field declares values that Claude Code prompts the user for when the plugin is enabled. Use this instead of requiring users to hand-edit `settings.json`.
 
-```json  theme={null}
+```json theme={null}
 {
   "userConfig": {
     "api_endpoint": {
@@ -381,7 +381,7 @@ Non-sensitive values are stored in `settings.json` under `pluginConfigs[<plugin-
 
 The `channels` field lets a plugin declare one or more message channels that inject content into the conversation. Each channel binds to an MCP server that the plugin provides.
 
-```json  theme={null}
+```json theme={null}
 {
   "channels": [
     {
@@ -409,7 +409,7 @@ For `skills`, `commands`, `agents`, `outputStyles`, and `monitors`, a custom pat
 
 **Path examples**:
 
-```json  theme={null}
+```json theme={null}
 {
   "commands": [
     "./specialized/deploy.md",
@@ -430,7 +430,7 @@ Claude Code provides two variables for referencing plugin paths. Both are substi
 
 **`${CLAUDE_PLUGIN_DATA}`**: a persistent directory for plugin state that survives updates. Use this for installed dependencies such as `node_modules` or Python virtual environments, generated code, caches, and any other files that should persist across plugin versions. The directory is created automatically the first time this variable is referenced.
 
-```json  theme={null}
+```json theme={null}
 {
   "hooks": {
     "PostToolUse": [
@@ -455,7 +455,7 @@ A common use is installing language dependencies once and reusing them across se
 
 This `SessionStart` hook installs `node_modules` on the first run and again whenever a plugin update includes a changed `package.json`:
 
-```json  theme={null}
+```json theme={null}
 {
   "hooks": {
     "SessionStart": [
@@ -476,7 +476,7 @@ The `diff` exits nonzero when the stored copy is missing or differs from the bun
 
 Scripts bundled in `${CLAUDE_PLUGIN_ROOT}` can then run against the persisted `node_modules`:
 
-```json  theme={null}
+```json theme={null}
 {
   "mcpServers": {
     "routines": {
@@ -505,6 +505,8 @@ For security and verification purposes, Claude Code copies *marketplace* plugins
 
 Each installed version is a separate directory in the cache. When you update or uninstall a plugin, the previous version directory is marked as orphaned and removed automatically 7 days later. The grace period lets concurrent Claude Code sessions that already loaded the old version keep running without errors.
 
+Claude's Glob and Grep tools skip orphaned version directories during searches, so file results don't include outdated plugin code.
+
 ### Path traversal limitations
 
 Installed plugins cannot reference files outside their directory. Paths that traverse outside the plugin root (such as `../shared-utils`) will not work after installation because those external files are not copied to the cache.
@@ -513,7 +515,7 @@ Installed plugins cannot reference files outside their directory. Paths that tra
 
 If your plugin needs to access files outside its directory, you can create symbolic links to external files within your plugin directory. Symlinks are preserved in the cache rather than dereferenced, and they resolve to their target at runtime. The following command creates a link from inside your plugin directory to a shared utilities location:
 
-```bash  theme={null}
+```bash theme={null}
 ln -s /path/to/shared-utils ./shared-utils
 ```
 
@@ -527,7 +529,7 @@ This provides flexibility while maintaining the security benefits of the caching
 
 A complete plugin follows this structure:
 
-```text  theme={null}
+```text theme={null}
 enterprise-plugin/
 ├── .claude-plugin/           # Metadata directory (optional)
 │   └── plugin.json             # plugin manifest
@@ -594,7 +596,7 @@ Claude Code provides CLI commands for non-interactive plugin management, useful
 
 Install a plugin from available marketplaces.
 
-```bash  theme={null}
+```bash theme={null}
 claude plugin install <plugin> [options]
 ```
 
@@ -613,7 +615,7 @@ Scope determines which settings file the installed plugin is added to. For examp
 
 **Examples:**
 
-```bash  theme={null}
+```bash theme={null}
 # Install to user scope (default)
 claude plugin install formatter@my-marketplace
 
@@ -628,7 +630,7 @@ claude plugin install formatter@my-marketplace --scope local
 
 Remove an installed plugin.
 
-```bash  theme={null}
+```bash theme={null}
 claude plugin uninstall <plugin> [options]
 ```
 
@@ -652,7 +654,7 @@ By default, uninstalling from the last remaining scope also deletes the plugin's
 
 Enable a disabled plugin.
 
-```bash  theme={null}
+```bash theme={null}
 claude plugin enable <plugin> [options]
 ```
 
@@ -671,7 +673,7 @@ claude plugin enable <plugin> [options]
 
 Disable a plugin without uninstalling it.
 
-```bash  theme={null}
+```bash theme={null}
 claude plugin disable <plugin> [options]
 ```
 
@@ -690,7 +692,7 @@ claude plugin disable <plugin> [options]
 
 Update a plugin to the latest version.
 
-```bash  theme={null}
+```bash theme={null}
 claude plugin update <plugin> [options]
 ```
 
@@ -781,7 +783,7 @@ This shows:
 
 **Correct structure**: Components must be at the plugin root, not inside `.claude-plugin/`. Only `plugin.json` belongs in `.claude-plugin/`.
 
-```text  theme={null}
+```text theme={null}
 my-plugin/
 ├── .claude-plugin/
 │   └── plugin.json      ← Only manifest here
@@ -806,7 +808,7 @@ If your components are inside `.claude-plugin/`, move them to the plugin root.
 
 Follow semantic versioning for plugin releases:
 
-```json  theme={null}
+```json theme={null}
 {
   "name": "my-plugin",
   "version": "2.1.0"
