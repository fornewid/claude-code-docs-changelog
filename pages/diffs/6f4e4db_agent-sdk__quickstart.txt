diff --git a/docs/agent-sdk__quickstart.md b/docs/agent-sdk__quickstart.md
index 2c85662..c76b181 100644
--- a/docs/agent-sdk__quickstart.md
+++ b/docs/agent-sdk__quickstart.md
@@ -204,15 +204,23 @@ The message handling inside the loop filters for human-readable output. Without
 Your agent is ready. Run it with the following command:
 
 <Tabs>
-  <Tab title="Python">
+  <Tab title="TypeScript">
     ```bash theme={null}
-    python3 agent.py
+    npx tsx agent.ts
     ```
   </Tab>
 
-  <Tab title="TypeScript">
+  <Tab title="Python (uv)">
     ```bash theme={null}
-    npx tsx agent.ts
+    uv run agent.py
+    ```
+  </Tab>
+
+  <Tab title="Python (pip)">
+    With your virtual environment still activated:
+
+    ```bash theme={null}
+    python agent.py
     ```
   </Tab>
 </Tabs>
