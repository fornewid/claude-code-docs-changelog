diff --git a/docs/agent-sdk__custom-tools.md b/docs/agent-sdk__custom-tools.md
index 2c8308c..858a059 100644
--- a/docs/agent-sdk__custom-tools.md
+++ b/docs/agent-sdk__custom-tools.md
@@ -359,8 +359,6 @@ The example below catches two kinds of failures inside the handler and composes
   from typing import Any
   from claude_agent_sdk import tool
 
-  from claude_agent_sdk import tool
-
 
   @tool(
       "fetch_data",
@@ -470,8 +468,6 @@ An image block carries the image bytes inline, encoded as base64. There is no UR
   import httpx
   from claude_agent_sdk import tool
 
-  from claude_agent_sdk import tool
-
 
   # Define a tool that fetches an image from a URL and returns it to Claude
   @tool("fetch_image", "Fetch an image from a URL and return it to Claude", {"url": str})
