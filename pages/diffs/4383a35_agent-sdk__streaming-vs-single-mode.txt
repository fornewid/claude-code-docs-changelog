diff --git a/docs/agent-sdk__streaming-vs-single-mode.md b/docs/agent-sdk__streaming-vs-single-mode.md
index b71383b..60a9646 100644
--- a/docs/agent-sdk__streaming-vs-single-mode.md
+++ b/docs/agent-sdk__streaming-vs-single-mode.md
@@ -91,17 +91,18 @@ sequenceDiagram
 
 <CodeGroup>
   ```typescript TypeScript theme={null}
-  import { query } from "@anthropic-ai/claude-agent-sdk";
+  import { query, type SDKUserMessage } from "@anthropic-ai/claude-agent-sdk";
   import { readFile } from "fs/promises";
 
-  async function* generateMessages() {
+  async function* generateMessages(): AsyncGenerator<SDKUserMessage> {
     // First message
     yield {
-      type: "user" as const,
+      type: "user",
       message: {
-        role: "user" as const,
+        role: "user",
         content: "Analyze this codebase for security issues"
-      }
+      },
+      parent_tool_use_id: null
     };
 
     // Wait for conditions or user input
@@ -109,9 +110,9 @@ sequenceDiagram
 
     // Follow-up with image
     yield {
-      type: "user" as const,
+      type: "user",
       message: {
-        role: "user" as const,
+        role: "user",
         content: [
           {
             type: "text",
@@ -126,7 +127,8 @@ sequenceDiagram
             }
           }
         ]
-      }
+      },
+      parent_tool_use_id: null
     };
   }
 
@@ -138,7 +140,7 @@ sequenceDiagram
       allowedTools: ["Read", "Grep"]
     }
   })) {
-    if (message.type === "result") {
+    if (message.type === "result" && message.subtype === "success") {
       console.log(message.result);
     }
   }
@@ -248,7 +250,7 @@ Use single message input when:
       allowedTools: ["Read", "Grep"]
     }
   })) {
-    if (message.type === "result") {
+    if (message.type === "result" && message.subtype === "success") {
       console.log(message.result);
     }
   }
@@ -261,7 +263,7 @@ Use single message input when:
       maxTurns: 1
     }
   })) {
-    if (message.type === "result") {
+    if (message.type === "result" && message.subtype === "success") {
       console.log(message.result);
     }
   }
